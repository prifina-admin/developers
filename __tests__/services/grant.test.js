const grant = require('../../src/services/grant');

// integration tests for the utility function grant
describe("Utility function \'grant\'", () => {
    todo('Given proper params, it grants permission to a service');
});