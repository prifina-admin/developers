// Tests for the request function

const request = require('../../src/access').request;

describe('Utility function \'request\'', () => {
    test('Throws error if service is not registered in the database', () => {

    });

    test('Throws error if user is not registered in the database', () => {

    });
    
    test('Given valid input, should add the request to the database', () => {

    });

    test('Given invalid input, should not add the request to the database', () => {

    });
});